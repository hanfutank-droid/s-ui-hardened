# Security

请不要在公开 Issue 中提交服务器地址、管理员凭据、订阅链接、私钥、数据库或完整配置。

默认面板和订阅服务仅监听本机地址。建议通过 SSH 隧道访问面板，或使用提供 HTTPS 和访问控制的反向代理；不要直接向公网开放管理端口。

本仓库不提供在线自动更新。升级时应审计固定版本源码，由 GitHub Actions 重新构建并验证 SHA-256。

发现疑似漏洞时，请使用 GitHub 仓库的私密安全报告功能联系维护者。
