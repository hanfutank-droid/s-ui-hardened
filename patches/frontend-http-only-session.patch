diff --git a/src/router/index.ts b/src/router/index.ts
index a894f18..7318b4f 100644
--- a/src/router/index.ts
+++ b/src/router/index.ts
@@ -86,21 +86,9 @@ const router = createRouter({
 const DEFAULT_TITLE = 'S-UI'
 let intervalId:any
 
-// Navigation guard to check authentication state
+// Authentication is enforced by the server and API. The session cookie is
+// intentionally HttpOnly, so client-side JavaScript must not inspect it.
 router.beforeEach((to) => {
-  // Check the session cookie
-  const sessionCookie = document.cookie.split(';').find(cookie => cookie.trim().startsWith('s-ui='))
-  const isAuthenticated = !!sessionCookie
-
-  // If the route requires authentication and the user is not authenticated, redirect to /login
-  if (to.meta.requiresAuth && !isAuthenticated) {
-    return '/login'
-  }
-  if (to.path === '/login' && isAuthenticated) {
-    // If already authenticated and visiting /login, redirect to '/'
-    return '/'
-  }
-
   // Load default data
   if (to.path !== '/login') {
     loadDataInterval()
